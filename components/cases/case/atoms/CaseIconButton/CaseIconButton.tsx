import React, { FC } from 'react'

import { CaseIconButtonProps } from './CaseIconButton.types'

export const CaseIconButton: FC<CaseIconButtonProps> = (props) => {
    const {} = props
    return <div>CaseIconButton</div>
}
